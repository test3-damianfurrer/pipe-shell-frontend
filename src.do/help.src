#!/bin/bash
functions=$(echo "${functions}";echo "help")
help(){
#echo "available Actions:"
#cat do.sh |  grep -A 1000 "action()"'{' | grep -m 1 -B 1000 "^"'}'  | grep "\")" | awk '{print $2}' FS='"'
echo "you can have a seperate player instance too:"
echo "simply set the env var PIP_MPV_SOCKET to a unique value"
echo "this will create a seperate mpv window with a sepertate ipc socket to controll it"
echo ""
echo "functions: ${functions}"
}

